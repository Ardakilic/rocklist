# API Configuration
# Choose either "lastfm" or "spotify"
API_SOURCE=lastfm

# Last.fm API credentials (required if API_SOURCE=lastfm)
LASTFM_API_KEY=your_lastfm_api_key
LASTFM_API_SECRET=your_lastfm_api_secret

# Spotify API credentials (required if API_SOURCE=spotify)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Rockbox configuration
# Path to the Rockbox directory
ROCKBOX_PATH=/path/to/.rockbox

# Path to store generated playlists (defaults to $ROCKBOX_PATH/playlists if not specified)
PLAYLIST_PATH=

# Comma-separated list of artists to generate playlists for (optional)
# If not specified, playlists will be generated for all artists in the database
ARTISTS=

# Maximum number of tracks per artist (defaults to 10)
MAX_TRACKS=10
